const assert = require('assert')
import User from '../models/User'

describe('Create', () => {
  it('Saves a user', () => {
    // const leo = new User({name: 'Leo'})
    // leo.save();

    assert(1 + 1 === 3)
  })
})